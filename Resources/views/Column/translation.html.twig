{% extends 'SgDatatablesBundle:Column:column.html.twig' %}

{% block data %}
    "render": function(data, type, row, meta) {
        var translation = row[{% for f in column.translation|split('.') %}"{{ f }}"][{% endfor %}0];
        return translation ? translation["{{ column.field }}"] : '';
    },
{% endblock %}
